# pnpm 10 no longer public-hoists by default; eslint-config-next resolves its
# bundled plugins (react-hooks, import, …) from the workspace root via
# FlatCompat, so they must be hoisted for `pnpm lint` to work.
public-hoist-pattern[]=*eslint*
